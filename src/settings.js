
/**
 * Common settings for jumpy.
 */
(function(settings) {
    settings.windowWidth = 1000;
    settings.windowHeight = 600;
    settings.radius = 50;
    settings.gravity = 1;
    settings.jumpForce = 18;
    settings.backgroundParticleCount = 150;
})(window.settings = window.settings || {});
